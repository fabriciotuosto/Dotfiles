session_name: wallet-cron
start_directory: ~/Projects/Intuitivo/wallet-cron
# shell_command_before:
# - '[ -f .venv/bin/activate ] && source .venv/bin/activate && reset'
windows:
- window_name: editor
  focus: true
  panes:
    - nvim .
- window_name: shell
