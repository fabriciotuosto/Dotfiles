session_name: firmware
start_directory: ~/Projects/Intuitivo/firmware-nerves
# shell_command_before:
# - '[ -f .venv/bin/activate ] && source .venv/bin/activate && reset'
windows:
- window_name: editor
  focus: true
  panes:
    - nvim .
- window_name: shell
