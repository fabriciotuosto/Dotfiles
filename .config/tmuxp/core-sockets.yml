session_name: sockets
start_directory: ~/Projects/Intuitivo/core-sockets
# shell_command_before:
# - '[ -f .venv/bin/activate ] && source .venv/bin/activate && reset'
windows:
- window_name: editor
  focus: true
  panes:
    - nvim .
- window_name: shell
  panes:
    - clear
- window_name: client
  panes:
    - shell_command:
      - cmd: cd ~/Projects/Intuitivo/core-sockets-client
      - cmd: nvim .
    - shell_command:
      - cmd: cd ~/Projects/Intuitivo/core-sockets-client
      - cmd: clear
